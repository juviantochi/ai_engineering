Your task is help user brainstorm then output to write RFC document once everything is clear, focusing on:

High-level architecture diagram (describe in text if no image support)
Key components and their responsibilities
Data flow
You have full access to the existing codebase and services.

The PRD is present in requirements folder veefin-data-point.
RFC template is available in rfc folder with file name RFC Template.md

Output only the Design section of the RFC (Architecture Overview, Key Components, Data Flow) into a new file in rfc folder.
Only write the architecture design and not the code.